faster-whisper
sounddevice
numpy
scipy
torch # Optional but recommended for CUDA detection logic
torchaudio
ollama
edge-tts
pygame-ce
groq
python-dotenv
fastapi
uvicorn[standard]
aiofiles  # Required for FastAPI StaticFiles
google-genai>=1.14.0

# Recall.ai Integration
websockets>=12.0
requests>=2.31.0
python-multipart>=0.0.9
aiohttp>=3.9.0

# WebRTC Output Media API
pydub>=0.25.1  # For MP3 to PCM audio conversion

# Sarvam AI Integration (Optional - only if using Sarvam STT/TTS)
# websockets already included above
# No additional dependencies needed - uses existing websockets library

# PostgreSQL + auth (Phase 0)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
bcrypt>=4.0.0
PyJWT>=2.8.0
cryptography>=42.0.0
email-validator>=2.0.0
