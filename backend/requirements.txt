# Pinned from backend/venv (Python 3.11.6) — 2026-07-15
# Install: pip install -r requirements.txt
# Torch CUDA build was installed as torch==2.5.1+cu121 from PyTorch index;
# plain 2.5.1 pins work for CPU. For CUDA, see https://pytorch.org/get-started/locally/

faster-whisper==1.2.1
sounddevice==0.5.5
numpy==2.4.4
scipy==1.17.1
torch==2.5.1
torchaudio==2.5.1
ollama==0.6.1
edge-tts==7.2.8
pygame-ce==2.5.7
groq==1.1.2
python-dotenv==1.2.2
fastapi==0.135.3
uvicorn[standard]==0.44.0
aiofiles==24.1.0  # Required for FastAPI StaticFiles (not previously installed in venv)
google-genai==1.73.1

# Recall.ai Integration
websockets==16.0
requests==2.33.1
python-multipart==0.0.32
aiohttp==3.13.5

# WebRTC Output Media API
pydub==0.25.1  # For MP3 to PCM audio conversion

# Sarvam AI Integration (Optional - only if using Sarvam STT/TTS)
# websockets already included above
# No additional dependencies needed - uses existing websockets library

# PostgreSQL + auth (Phase 0)
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
bcrypt==5.0.0
PyJWT==2.13.0
cryptography==46.0.7
email-validator==2.3.0

# Camera / face detection (local test + future bot integration)
opencv-python==5.0.0.93
mediapipe==0.10.35
