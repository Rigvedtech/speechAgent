# Local scratch: certs, keys, PFX exports — never commit secrets
temp/
temp.rar

# DB dumps from deploy migrate (schema + data) — never commit
backups/

# OS / editor noise at repo root
.DS_Store
Thumbs.db

frontend/node_modules
frontend/dist